target-version = "py311"
line-length = 100

[lint]
select = ["E", "F", "I", "W", "UP"]
ignore = ["E501"]  # line length enforced by formatter, not checker

[lint.per-file-ignores]
"tests/**" = ["F811"]  # allow fixture redefinition in tests
